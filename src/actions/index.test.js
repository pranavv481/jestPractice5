import {actionTypes} from "./index";

describe('correctGuess', ()=>{
    test('action with type `CORRECT_GUESS`', ()=>{
        // const action = correctGuess();
        // expect(action).toEqual({type:actionTypes.CORRECT_GUESS})
        
    })
})